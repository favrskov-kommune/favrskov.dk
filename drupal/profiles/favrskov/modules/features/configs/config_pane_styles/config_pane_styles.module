<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function config_pane_styles_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner == 'panels' && $plugin_type == 'styles') {
    return 'plugins/' . $plugin_type;
  }
}