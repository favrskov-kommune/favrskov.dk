<?php
/**
 * @file
 * Extend Linkit with panel page links
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function  linkit_panel_pages_ctools_plugin_directory($module, $plugin) {
  if ($module == 'linkit' && !empty($plugin)) {
    return "plugins/" . $plugin;
  }
}